import React from "react";

import {CameraViewManager} from './nativeUIModule'

type CameraProps = {
    devId : any;
}

// const CameraView = ({devId} : CameraProps) => {
//     return <CameraViewManager devId={devId} />
// }